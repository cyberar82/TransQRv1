<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin" android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="#0099cc"
    tools:context="com.example.spalacio.transqrv1.MainActivity">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->

    <Button
        android:id="@+id/btnScann"
        style="?metaButtonBarButtonStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/LightSlateGray"
        android:text="@string/btnScann_title" android:layout_centerHorizontal="true" android:textColor="@color/Red"  />
    <TextView android:id="@+id/logError"
        android:layout_width="wrap_content" android:layout_height="wrap_content" />

    <ListView android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:scrollbarSize="@dimen/activity_vertical_margin"
        android:background="@color/White"
        android:scrollbars="vertical"
        android:id="@+id/listQRReader"
        android:layout_below="@id/btnScann" />
</RelativeLayout>
